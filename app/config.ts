// Client-safe config — only VITE_-prefixed vars here (baked into client bundle).
// Server-only secrets stay in process.env, accessed from *.server.ts modules.
export const ASSET_CDN_BASE_URL = "https://d3n1g0yg3ja4p3.cloudfront.net";
